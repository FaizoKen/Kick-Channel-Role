services:
  app:
    build: .
    ports:
      - "8094:8094"
    environment:
      DATABASE_URL: postgres://kcr:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD env var must be set}@db:5432/kick_channel_role
      SESSION_SECRET: ${SESSION_SECRET:?SESSION_SECRET env var must be set}
      BASE_URL: ${BASE_URL:?BASE_URL env var must be set}
      LISTEN_ADDR: "0.0.0.0:8094"
      AUTH_GATEWAY_URL: ${AUTH_GATEWAY_URL}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY:?INTERNAL_API_KEY env var must be set}
      ROLELOGIC_API_URL: ${ROLELOGIC_API_URL:-}
      RL_DASHBOARD_ORIGIN: ${RL_DASHBOARD_ORIGIN:-}
      # Kick OAuth + webhook credentials are loaded in Phase 3+, listed here
      # so compose surfaces them as missing rather than letting the binary
      # panic at first OAuth call.
      KICK_CLIENT_ID: ${KICK_CLIENT_ID:-}
      KICK_CLIENT_SECRET: ${KICK_CLIENT_SECRET:-}
      KICK_WEBHOOK_SECRET: ${KICK_WEBHOOK_SECRET:-}
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - curl
        - --fail
        - --silent
        - --max-time
        - "2"
        - http://127.0.0.1:8094/kick-channel-role/health
      interval: 15s
      timeout: 3s
      start_period: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 128M

  db:
    image: postgres:16-alpine
    command: >
      postgres
      -c shared_buffers=64MB
      -c work_mem=2MB
      -c maintenance_work_mem=32MB
      -c effective_cache_size=128MB
      -c max_connections=15
      -c wal_buffers=4MB
      -c checkpoint_completion_target=0.9
      -c random_page_cost=1.1
    environment:
      POSTGRES_DB: kick_channel_role
      POSTGRES_USER: kcr
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD env var must be set}
    volumes:
      - pgdata:/var/lib/postgresql/data
    shm_size: '64m'
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kcr -d kick_channel_role"]
      interval: 5s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 256M

volumes:
  pgdata:
